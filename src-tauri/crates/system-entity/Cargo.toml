[package]
name = "system-entity"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", features = ["clock"] }
serde = { version = "1.0", features = ["derive"] }
ulid = "1.1.2"

[dependencies.sea-orm]
version = "2.0.0-rc.38"
